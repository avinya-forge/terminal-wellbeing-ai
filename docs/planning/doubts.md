# doubts

- [BLOCKER] **DB choice missing**: We need to know which database to use (e.g., Postgres, MongoDB, SQLite) before we can "Analyze DB schema integration for persistent chat history" and "Drill down into schema definition".
- [BLOCKER] **OAuth provider choice missing**: We need to know which OAuth provider to use (e.g., Auth0, Google, GitHub) before we can "Implement OAuth for user session storage" and "Configure callback URLs and tokens".
