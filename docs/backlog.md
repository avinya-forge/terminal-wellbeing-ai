# Backlog

## Current Tasks
- [x] Refactor `aiModel.ts` to separate configuration and data.
- [x] Add unit tests for `aiModel.ts` logic (`isSensitiveTopic`, `sanitizePromptContent`).
- [ ] Fix potential bug in command processing overlap between `Terminal.tsx` and `commands.ts`.

## Future Improvements
- [ ] Implement local storage for conversation history (optional persistence).
- [ ] Add accessibility features (ARIA labels) to the terminal interface.
- [ ] Create a "Panic" mode for immediate crisis resource display.
- [ ] Optimize model loading time and feedback.
